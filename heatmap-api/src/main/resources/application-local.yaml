spring:
  jpa:
    show-sql: true

app:
  security:
    cookie:
      secure: false

logging:
  level:
    root: INFO
    ru.tbank.tmap: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE